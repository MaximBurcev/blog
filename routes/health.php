<?php

use App\Http\Controllers\HealthController;
use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Health-check
|--------------------------------------------------------------------------
|
| Отдельный файл, а не строка в web.php: маршрут подключается вне групп web и
| api (см. RouteServiceProvider), чтобы не тянуть сессию, cookie и CSRF.
| Монитор ходит сюда раз в минуту, и при SESSION_DRIVER=file каждый такой
| запрос оставлял бы файл сессии в shared-каталоге.
|
| Throttle тоже не вешаем: упереться в лимитер монитор не должен, иначе
| «сервис недоступен» окажется ложной тревогой от нашего же ограничителя.
|
*/

Route::get('/up', HealthController::class)->name('health');
