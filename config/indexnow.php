<?php

return [
    /*
    |--------------------------------------------------------------------------
    | IndexNow — мгновенное уведомление поисковиков о новых страницах
    |--------------------------------------------------------------------------
    |
    | При публикации поста его адрес уходит POST'ом на api.indexnow.org —
    | Яндекс и Bing узнают о странице за минуты, а не при очередном обходе.
    | Google протокол не поддерживает.
    |
    | Ключ подтверждает владение сайтом: его содержимое отдаётся по адресу
    | /{key}.txt (маршрут в web.php). Пустой ключ — фича молча выключена,
    | маршрут ключа при этом не регистрируется.
    |
    */

    'enabled' => env('INDEXNOW_ENABLED', true),

    'key' => env('INDEXNOW_KEY'),

    'endpoint' => env('INDEXNOW_ENDPOINT', 'https://api.indexnow.org/indexnow'),
];
