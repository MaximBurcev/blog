<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Адрес админки (Filament)
    |--------------------------------------------------------------------------
    |
    | env() читается только здесь: на проде конфиг кэшируется (envoy deploy
    | → config:cache), и env() в провайдере после кэширования вернул бы null.
    |
    | В robots.txt адрес НЕ добавляем: Disallow-строка лишь сообщала бы ботам
    | новый путь. Панель и так закрыта авторизацией, а адрес не публикуем.
    |
    */

    'panel_path' => env('ADMIN_PANEL_PATH', 'filament'),
];
